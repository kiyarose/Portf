# Cloudflare Pages serves 404.html and 500.html automatically—no explicit
# redirects needed. These rules only handle SPA routing.

# Proxy remote data requests to data.kiya.cat to avoid CORS issues
/data/* https://data.kiya.cat/data/:splat 200

/privacy-policy /index.html 200

/tools/convert 200
/tools/visualizeme 200
# Preserve the legacy slug while the new route propagates.

/tools/visualiseme /src/tools/visualizeme.html 301
/tools/* /src/tools/:splat 200

# Fallback all other extension-less routes to the SPA entry point.
/* /index.html 200!
