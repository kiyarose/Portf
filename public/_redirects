# Cloudflare Pages serves 404.html and 500.html automatically—no explicit
# redirects needed. These rules only handle SPA routing.

/privacy-policy /index.html 200

/tools/visualiseme /tools/visualizeme 200
/tools/* /tools/:splat 200

# Fallback all other extension-less routes to the SPA entry point.
/* /app-shell 200
